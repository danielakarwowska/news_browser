import React from 'react'
import App from '../article_floating'
import {render} from '@testing-library/react'
describe('App component', () => {
    it('some text', () => {
        expect('');
    })
})