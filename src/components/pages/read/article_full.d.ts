import { Article } from '../../../types';
type Props = {
    selectedArticle: Article | undefined;
};
declare const PageSliceArticleFull: ({ selectedArticle }: Props) => JSX.Element;
export default PageSliceArticleFull;
