import React from 'react'
import {render} from '@testing-library/react'
import axios from 'axios'
jest.mock('axios')
describe('my test', () => {
    it('some text', () => {
        expect('');
    })
})